# CoreML conversion dependencies for GhostType
# Install: pip install -r requirements.txt

# Core ML tools (Apple's official converter)
coremltools>=8.0

# PyTorch and related
torch>=2.4.0
torchaudio>=2.4.0
numpy>=1.26.0

# HuggingFace Transformers (for Moonshine model)
# Note: Moonshine was added to transformers in v4.48+
transformers>=4.48.0

# Tokenizer dependencies
sentencepiece>=0.2.0
safetensors>=0.4.5

# Audio processing (for validation)
soundfile>=0.12.0
librosa>=0.10.0